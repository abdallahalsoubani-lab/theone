# `/api/v1`

REST endpoints for external clients (mobile, third-party integrations) and webhooks.
Internal mutations use server actions instead — see Prompt 0 §5.4.

Endpoints are added by the prompt that owns the relevant module (see `docs/Theone-pt-Technical-Spec.md` §9).
