# `messages`

i18n catalogs (`en.json`, `ar.json`). Owned by **Prompt 3 — Internationalization**.

CI enforces key-by-key parity between `en.json` and `ar.json` — see `.github/workflows/sync-check.yml`.
